 TEsts TEsts and more tests 

make depend 
make install

Web: cronjob pour générer la  doc et la page
fix Makefile to be able to either link the doc  to local or remote doc

--

Var: 2°
    make a simple graph var


Template nodearcGraphView with the arcnode parameter 
    Design a simple Tail*MumNodes+Head ArcNode (must know numNodes)

Path:
    remove weakly connected checking
    use smart pointer for the costs
    condensation des morceaux de chemin/retrait des loops == partie des degrés + Nocycle


ArcSetView<GView>

Gecode:
automativcally enable tagfile=yes if contribs are done and doc is enabled
int minimodel = q[0](intvar) = 3 should work
